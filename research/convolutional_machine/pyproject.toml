[project]
name = "crbm-repository"
version = "0.1.0"
description = "Convolutional Restricted Boltzmann Machine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.22.0",
    "scipy>=1.8.0",
]

[tool.uv]
managed = true
