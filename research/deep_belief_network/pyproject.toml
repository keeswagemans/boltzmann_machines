[project]
name = "dbn-repository"
version = "0.1.0"
description = "Deep Belief Network implemented as a stack of RBMs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.22.0",
]

[tool.uv]
managed = true
