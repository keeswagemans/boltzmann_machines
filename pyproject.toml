[project]
name = "rbm-repository"
version = "0.1.0"
description = "Restricted Boltzmann Machine from scratch"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.22.0",
    "matplotlib>=3.5.0",
]

[tool.uv]
managed = true
